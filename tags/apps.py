from django.apps import AppConfig


class TagsConfig(AppConfig):
    """Class representing tags application and its configuration."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'tags'
