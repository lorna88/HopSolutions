from django.apps import AppConfig


class TaskCalendarConfig(AppConfig):
    """Class representing task_calendar application and its configuration."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'task_calendar'
